app = "ai-hub-vm-server-dev"
primary_region = "nrt"

[build]
  dockerfile = "Dockerfile.dev"

[env]
  NODE_ENV = "development"
  PORT = "4000"
  HOST = "0.0.0.0"
  DATA_DIR = "/data"
  POSTGRES_USER = "aihub"
  POSTGRES_DB = "aihub_dev"

[http_service]
  internal_port = 4000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[mounts]
  source = "vm_data_dev"
  destination = "/data"

[checks]
  [checks.health]
    type = "http"
    port = 4000
    path = "/health"
    interval = "30s"
    timeout = "5s"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
